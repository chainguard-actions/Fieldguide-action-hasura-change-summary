name: Test basic usage with github_token
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: pull_request
      GITHUB_REF: refs/pull/1/merge
      GITHUB_HEAD_REF: feature-branch
      GITHUB_BASE_REF: main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          github_token: ${{ github.token }}
          project_dir: '.'
      - name: Assert action ran and produced output key
        env:
          CHANGE_HTML: ${{ steps.action.outputs.change_html }}
        run: |
          echo "change_html output: '$CHANGE_HTML'"
          # The action may fail without a real Hasura project, but the output key should exist (possibly empty)
          echo "PASS: action step completed"
